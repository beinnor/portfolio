import styled from 'styled-components';

export const Social = styled.div`
  svg {
    color: #fafafa;
  }
`;

export const HiddenText = styled.p`
  border: 0;
  clip: rect(0 0 0 0);
  height: 1px;
  margin: -1px;
  overflow: hidden;
  padding: 0;
  position: absolute;
  width: 1px;
`;
